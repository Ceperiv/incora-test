import React from 'react';

function ViewsByShowNames({showInfo}) {

    // const watch = () => {
    //     const show = showInfo.find(value => value.title === showName);
    //
    //     if (!show) {
    //         throw new Error('Show not found!!!')
    //     }
    //
    //     streamingService.viewsByShowNames.set(show.name, (streamingService.viewsByShowNames.get(show.name) || 0) + 1)
    //     return show
    // }
    return (
        <div>
            ViewsByShowNames
        </div>

    );
}

export {ViewsByShowNames};